import{_ as s,o as d,c as o,b0 as t}from"./chunks/framework.DDuwZS-e.js";const _=JSON.parse('{"title":"账号密码","description":"","frontmatter":{"title":"账号密码"},"headers":[],"relativePath":"DB/redis/auth.md","filePath":"DB/redis/auth.md","lastUpdated":1737636722000}'),a={name:"DB/redis/auth.md"};function c(i,e,p,r,l,n){return d(),o("div",null,e[0]||(e[0]=[t('<p><code>Redis</code> 默认地址是 <code>127.0.0.1:6379</code>。</p><p>当使用 <code>redis-cli</code> 时，默认是不需要密码的。</p><p>如果想要更加安全，那么可以给 <code>Redis</code> 添加密码验证。</p><p>首先找到 <code>Redis</code> 的配置文件 <code>/etc/redis.conf</code> 或者 <code>/usr/local/etc/redis.conf</code>，</p><p>在该文件中查找 <code>requirepass</code> 字段，默认如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># requirepass foobared</span></span></code></pre></div><p>将以上配置修改为：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>requirepass &lt;password&gt;</span></span></code></pre></div><p>利用 <code>brew services restart redis</code> 重启 <code>Redis</code> 之后，配置生效。</p><p>此后，登录 <code>Redis</code> 有以下两种方式：</p><ol><li><code>redis-cli -a &lt;password&gt;</code></li><li><code>redis-cli</code> 之后，然后执行 <code>AUTH &lt;password&gt;</code>。（<strong>推荐</strong>）</li></ol>',11)]))}const h=s(a,[["render",c]]);export{_ as __pageData,h as default};
